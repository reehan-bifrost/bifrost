- fix: drop namespace tools whose name Amazon Bedrock reserves (`web`, `image_gen`, `browser`, `python`) on the Bedrock and Bedrock Mantle Responses paths instead of forwarding them into a `tools.namespace` collision 400; a dropped Codex `web` namespace becomes the hosted `web_search` tool on Bedrock Mantle
- feat: model allow and block lists gain pattern twins. `Key` carries `models_patterns` and `blacklisted_models_patterns`, `ProviderPermit` carries `allowed_models_patterns` and `blacklisted_models_patterns`, and `schemas.ModelAccessRule` evaluates exact lists and RE2 patterns together (full match, case-insensitive, tried against the model name and `provider/model`; block wins). `WhiteList` and `BlackList` stay exact. The provider list-models pipeline takes the rule so a patterns-only key still lists what it can serve
